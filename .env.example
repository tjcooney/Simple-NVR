# Required password for web interface login
WEBSITE_PASSWORD=your_password_here

# Title shown in the web interface
WEBSITE_TITLE=Simple NVR

# Secret key for Flask sessions (use a random string in production)
SECRET_KEY=change_this_to_a_random_string

# Optional: Flask port (defaults to 5001 if not set)
FLASK_PORT=5001
